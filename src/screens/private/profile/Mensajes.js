import React from 'react';
import {
	ImageBackground,
	Text,
	TouchableOpacity,
	View,
	Image
} from 'react-native';

const Mensajes = (props) => {
	return (
		<View
				style={{
					backgroundColor: '#FAFAFF',
					padding: 20,
					borderRadius: 10,
					marginBottom: 15,
					flex: 1,
					shadowColor: "#000",
					shadowOffset: {
						width: 0,
						height: 5,
					},
					shadowOpacity: 0.36,
					shadowRadius: 6.68,

					elevation: 11,
				}}
			>
				
			</View>
	);
};

export default Mensajes;
